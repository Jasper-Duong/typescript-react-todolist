import React from "react";
import styled from "styled-components";

const Completed: React.FC = () => {
  return <Styled className="todos completed">
    <h3>Completed</h3>
  </Styled>;
};

export default Completed;
const Styled = styled.div``;